server:
  url: "http://fl-server:8080"

training:
  local_epochs: 5
  batch_size: 32
  learning_rate: 0.01

data:
  path: /data/dataset.csv
  test_split: 0.2
  num_classes: 2   # must match the value passed to POST /start on the server
